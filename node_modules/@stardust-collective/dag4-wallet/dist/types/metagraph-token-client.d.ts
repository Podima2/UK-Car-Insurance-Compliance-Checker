import { PostTransactionV2 } from "@stardust-collective/dag4-keystore";
import { ActionType, AllowSpend, MetagraphNetworkInfo, MetagraphTokenNetwork, PendingTx, TokenLock, TransactionReference } from "@stardust-collective/dag4-network";
import type { DagAccount } from "./dag-account";
declare class MetagraphTokenClient {
    private account;
    private networkInfo;
    private tokenDecimals;
    private network;
    constructor(account: DagAccount, networkInfo: MetagraphNetworkInfo, tokenDecimals?: number);
    get networkInstance(): MetagraphTokenNetwork;
    get address(): string;
    getTransactions(limit?: number, searchAfter?: string): Promise<import("@stardust-collective/dag4-network").TransactionV2[]>;
    getActions(actionType?: ActionType, limit?: number, searchAfter?: string, searchBefore?: string, next?: string): Promise<import("@stardust-collective/dag4-network").ActionResponse[]>;
    getBalance(): Promise<number>;
    getBalanceFor(address: string): Promise<number>;
    getLockedBalance(): Promise<number>;
    getFeeRecommendation(): Promise<number>;
    transfer(toAddress: string, amount: number, fee?: number, autoEstimateFee?: boolean, params?: Record<string, any>): Promise<PendingTx>;
    waitForBalanceChange(initialValue?: number): Promise<boolean>;
    private wait;
    generateBatchTransactions(transfers: TransferBatchItem[], lastRef?: TransactionReference): Promise<any[]>;
    sendBatchTransactions(transactions: PostTransactionV2[]): Promise<any[]>;
    transferBatch(transfers: TransferBatchItem[], lastRef?: TransactionReference): Promise<any[]>;
    createAllowSpend(body: AllowSpend, params?: Record<string, any>): Promise<import("@stardust-collective/dag4-network").HashResponse>;
    createTokenLock(body: TokenLock, params?: Record<string, any>): Promise<import("@stardust-collective/dag4-network").HashResponse>;
    getDataFeeEstimate(data: any): Promise<import("@stardust-collective/dag4-network").EstimateFeeResponse>;
    sendDataTransaction(data: any): Promise<import("@stardust-collective/dag4-network").SendDataFeeResponse>;
}
declare type TransferBatchItem = {
    address: string;
    amount: number;
    fee?: number;
};
export { MetagraphTokenClient };
