import { type PendingTx, type Transaction, type TransactionV2 } from '@stardust-collective/dag4-network';
import { Subject } from 'rxjs';
import { DagAccount } from './dag-account';
export declare class DagMonitor {
    private dagAccount;
    private memPoolChange$;
    private lastTimer;
    private pendingTimer;
    private waitForMap;
    constructor(dagAccount: DagAccount);
    observeMemPoolChange(): Subject<DagWalletMonitorUpdate>;
    addToMemPoolMonitor(value: PendingTx | string): Promise<Transaction | TransactionV2>;
    getLatestTransactions(address: string, limit?: number, searchAfter?: string): Promise<(Transaction | TransactionV2)[]>;
    getMemPoolFromMonitor(address?: string): Promise<PendingTx[]>;
    setToMemPoolMonitor(pool: PendingTx[]): Promise<void>;
    waitForTransaction(hash: string): Promise<boolean>;
    startMonitor(): void;
    private concatPendingTransactions;
    private transformPendingToTransaction;
    private pollPendingTxs;
    private processPendingTxs;
    private get cacheUtils();
}
export declare type DagWalletMonitorUpdate = {
    pendingHasConfirmed: boolean;
    transTxs: PendingTx[];
    txChanged: boolean;
};
